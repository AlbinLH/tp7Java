package tp7;

public class RPN {
	public static double interpret(String s) {
		String tab[] = s.split(" ");
		
		String[] tab_opé = new String[0];
		for (int i = 0; i < tab.length; i++) {
			if (tab[i].equals("+") || tab[i].equals("-") || tab[i].equals("*") || tab[i].equals("/")) {
				
			}
		}
		
		
		
		
		
		
		
		
		
		return();
				
	}
}
